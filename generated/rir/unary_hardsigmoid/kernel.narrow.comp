// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: unary_hardsigmoid (backend vulkan, reduction serial, scan serial, variante narrow).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 32, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float x[]; };
layout(std430, binding = 1) buffer Buf1 { float dst[]; };

layout(push_constant) uniform PC {
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint x_nb0;
    uint x_nb1;
    uint x_nb2;
    uint x_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x * 4u;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const uint base_v29 = row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2;
    const float c_v17 = 3.0;
    const float c_v19 = 6.0;
    const float c_v21 = 0.0;
    const float c_v24 = 1.0;
    const uint base_v30 = row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    const uint base_v31 = col_v0 * pc.x_nb0 + row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2;
    const uint base_v32 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const uint base_v27 = batch_v3 * pc.x_nb3 + base_v29;
        const uint base_v28 = batch_v3 * pc.dst_nb3 + base_v30;
        if (col_v0 + 4u <= pc.n_col) {
            const uint t_v4_i = (batch_v3 * pc.x_nb3 + base_v31) / 4u;
            const vec4 t_v4 = vec4(x[t_v4_i], x[t_v4_i + 1u], x[t_v4_i + 2u], x[t_v4_i + 3u]);
            const vec4 t_v6 = t_v4 + c_v17;
            const vec4 t_v8 = t_v6 / c_v19;
            const bvec4 b_v10 = greaterThan(t_v8, vec4(c_v21));
            const vec4 t_v11 = mix(vec4(c_v21), t_v8, b_v10);
            const bvec4 b_v13 = lessThan(t_v11, vec4(c_v24));
            const vec4 t_v14 = mix(vec4(c_v24), t_v11, b_v13);
            const uint t_v14_o = (batch_v3 * pc.dst_nb3 + base_v32) / 4u;
            dst[t_v14_o] = t_v14.x;
            dst[t_v14_o + 1u] = t_v14.y;
            dst[t_v14_o + 2u] = t_v14.z;
            dst[t_v14_o + 3u] = t_v14.w;
        } else {
            for (uint col_tail_v15 = col_v0; col_tail_v15 < pc.n_col; ++col_tail_v15) {
                const float t_v16 = x[(col_tail_v15 * pc.x_nb0 + base_v27) / 4u];
                const float t_v18 = t_v16 + c_v17;
                const float t_v20 = t_v18 / c_v19;
                const bool b_v22 = t_v20 > c_v21;
                const float t_v23 = b_v22 ? t_v20 : c_v21;
                const bool b_v25 = t_v23 < c_v24;
                const float t_v26 = b_v25 ? t_v23 : c_v24;
                dst[(col_tail_v15 * pc.dst_nb0 + base_v28) / 4u] = t_v26;
            }
        }
    }
}
