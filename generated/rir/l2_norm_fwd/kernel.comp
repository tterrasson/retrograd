// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: l2_norm_fwd (backend vulkan, reduction subgroup_tree, scan serial, variante repli).
// Strides are bytes in ggml order; parallel axes are mapped by workgroup.
#version 450
#extension GL_KHR_shader_subgroup_arithmetic : require

layout(local_size_x = 32, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float x[]; };
layout(std430, binding = 1) buffer Buf1 { float y[]; };

layout(push_constant) uniform PC {
    uint n_row;
    uint n_col;
    uint x_nb0;
    uint x_nb1;
    uint y_nb0;
    uint y_nb1;
} pc;

void main() {
    const uint row_v0 = gl_WorkGroupID.x;
    if (row_v0 >= pc.n_row) {
        return;
    }
    const uint lane_v1 = gl_LocalInvocationID.x;
    float acc4_v2 = 0.0;
    for (uint col_v3 = lane_v1; col_v3 < pc.n_col; col_v3 += 32u) {
        const float t_v4 = x[(col_v3 * pc.x_nb0 + row_v0 * pc.x_nb1) / 4u];
        const float t_v5 = t_v4 * t_v4;
        acc4_v2 += t_v5;
    }
    const float red4_v6 = subgroupAdd(acc4_v2);
    const float t_v9 = sqrt(red4_v6);
    for (uint col_v7 = lane_v1; col_v7 < pc.n_col; col_v7 += 32u) {
        const float t_v8 = x[(col_v7 * pc.x_nb0 + row_v0 * pc.x_nb1) / 4u];
        const float t_v10 = t_v8 / t_v9;
        y[(col_v7 * pc.y_nb0 + row_v0 * pc.y_nb1) / 4u] = t_v10;
    }
}
