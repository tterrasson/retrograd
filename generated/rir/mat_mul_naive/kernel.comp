// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: mat_mul_naive (backend vulkan, reduction serial, scan serial, variante repli).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 16, local_size_y = 16, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float a[]; };
layout(std430, binding = 1) readonly buffer Buf1 { float b[]; };
layout(std430, binding = 2) buffer Buf2 { float c[]; };

layout(push_constant) uniform PC {
    uint n_i;
    uint n_j;
    uint n_k;
    uint a_nb0;
    uint a_nb1;
    uint b_nb0;
    uint b_nb1;
    uint c_nb0;
    uint c_nb1;
} pc;

void main() {
    const uint i_v0 = gl_GlobalInvocationID.x;
    if (i_v0 >= pc.n_i) {
        return;
    }
    const uint j_v1 = gl_GlobalInvocationID.y;
    if (j_v1 >= pc.n_j) {
        return;
    }
    float acc6_v2 = 0.0;
    for (uint k_v3 = 0u; k_v3 < pc.n_k; ++k_v3) {
        const float t_v4 = a[(k_v3 * pc.a_nb0 + i_v0 * pc.a_nb1) / 4u];
        const float t_v5 = b[(k_v3 * pc.b_nb0 + j_v1 * pc.b_nb1) / 4u];
        const float t_v6 = t_v4 * t_v5;
        acc6_v2 += t_v6;
    }
    c[(i_v0 * pc.c_nb0 + j_v1 * pc.c_nb1) / 4u] = acc6_v2;
}
