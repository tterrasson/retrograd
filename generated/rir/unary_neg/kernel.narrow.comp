// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: unary_neg (backend vulkan, reduction serial, scan serial, variante narrow).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 32, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float x[]; };
layout(std430, binding = 1) buffer Buf1 { float dst[]; };

layout(push_constant) uniform PC {
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint x_nb0;
    uint x_nb1;
    uint x_nb2;
    uint x_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x * 4u;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const float c_v8 = 0.0;
    const uint base_v13 = row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2;
    const uint base_v14 = row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    const uint base_v15 = col_v0 * pc.x_nb0 + row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2;
    const uint base_v16 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const uint base_v11 = batch_v3 * pc.x_nb3 + base_v13;
        const uint base_v12 = batch_v3 * pc.dst_nb3 + base_v14;
        if (col_v0 + 4u <= pc.n_col) {
            const uint t_v5_i = (batch_v3 * pc.x_nb3 + base_v15) / 4u;
            const vec4 t_v5 = vec4(x[t_v5_i], x[t_v5_i + 1u], x[t_v5_i + 2u], x[t_v5_i + 3u]);
            const vec4 t_v6 = c_v8 - t_v5;
            const uint t_v6_o = (batch_v3 * pc.dst_nb3 + base_v16) / 4u;
            dst[t_v6_o] = t_v6.x;
            dst[t_v6_o + 1u] = t_v6.y;
            dst[t_v6_o + 2u] = t_v6.z;
            dst[t_v6_o + 3u] = t_v6.w;
        } else {
            for (uint col_tail_v7 = col_v0; col_tail_v7 < pc.n_col; ++col_tail_v7) {
                const float t_v9 = x[(col_tail_v7 * pc.x_nb0 + base_v11) / 4u];
                const float t_v10 = c_v8 - t_v9;
                dst[(col_tail_v7 * pc.dst_nb0 + base_v12) / 4u] = t_v10;
            }
        }
    }
}
