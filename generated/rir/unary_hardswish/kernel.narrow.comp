// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: unary_hardswish (backend vulkan, reduction serial, scan serial, variante narrow).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 32, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float x[]; };
layout(std430, binding = 1) buffer Buf1 { float dst[]; };

layout(push_constant) uniform PC {
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint x_nb0;
    uint x_nb1;
    uint x_nb2;
    uint x_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x * 4u;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const float c_v18 = 3.0;
    const float c_v20 = 6.0;
    const float c_v22 = 0.0;
    const float c_v25 = 1.0;
    const uint base_v31 = col_v0 * pc.x_nb0 + row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2;
    const uint base_v32 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const uint base_v29 = row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2 + batch_v3 * pc.x_nb3;
        const uint base_v30 = row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2 + batch_v3 * pc.dst_nb3;
        if (col_v0 + 4u <= pc.n_col) {
            const uint t_v4_i = (batch_v3 * pc.x_nb3 + base_v31) / 4u;
            const vec4 t_v4 = vec4(x[t_v4_i], x[t_v4_i + 1u], x[t_v4_i + 2u], x[t_v4_i + 3u]);
            const vec4 t_v6 = t_v4 + c_v18;
            const vec4 t_v8 = t_v6 / c_v20;
            const bvec4 b_v10 = greaterThan(t_v8, vec4(c_v22));
            const vec4 t_v11 = mix(vec4(c_v22), t_v8, b_v10);
            const bvec4 b_v13 = lessThan(t_v11, vec4(c_v25));
            const vec4 t_v14 = mix(vec4(c_v25), t_v11, b_v13);
            const vec4 t_v15 = t_v4 * t_v14;
            const uint t_v15_o = (batch_v3 * pc.dst_nb3 + base_v32) / 4u;
            dst[t_v15_o] = t_v15.x;
            dst[t_v15_o + 1u] = t_v15.y;
            dst[t_v15_o + 2u] = t_v15.z;
            dst[t_v15_o + 3u] = t_v15.w;
        } else {
            for (uint col_tail_v16 = col_v0; col_tail_v16 < pc.n_col; ++col_tail_v16) {
                const float t_v17 = x[(col_tail_v16 * pc.x_nb0 + base_v29) / 4u];
                const float t_v19 = t_v17 + c_v18;
                const float t_v21 = t_v19 / c_v20;
                const bool b_v23 = t_v21 > c_v22;
                const float t_v24 = b_v23 ? t_v21 : c_v22;
                const bool b_v26 = t_v24 < c_v25;
                const float t_v27 = b_v26 ? t_v24 : c_v25;
                const float t_v28 = t_v17 * t_v27;
                dst[(col_tail_v16 * pc.dst_nb0 + base_v30) / 4u] = t_v28;
            }
        }
    }
}
