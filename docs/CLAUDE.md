The documentation site is VitePress, driven by Bun: `bun install`, then
`bun run docs:dev` to preview and `bun run docs:build` to build (see `README.md`).
`docs:build` fails on a dead link between pages, so run it after moving a page.
