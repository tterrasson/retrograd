// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: unary_tanh (backend vulkan, reduction serial, scan serial, variante repli).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float x[]; };
layout(std430, binding = 1) buffer Buf1 { float dst[]; };

layout(push_constant) uniform PC {
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint x_nb0;
    uint x_nb1;
    uint x_nb2;
    uint x_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x * 4u;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const uint base_v11 = col_v0 * pc.x_nb0 + row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2;
    const uint base_v12 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const uint base_v9 = row_v1 * pc.x_nb1 + plane_v2 * pc.x_nb2 + batch_v3 * pc.x_nb3;
        const uint base_v10 = row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2 + batch_v3 * pc.dst_nb3;
        if (col_v0 + 4u <= pc.n_col) {
            const uint t_v4_i = (batch_v3 * pc.x_nb3 + base_v11) / 4u;
            const vec4 t_v4 = vec4(x[t_v4_i], x[t_v4_i + 1u], x[t_v4_i + 2u], x[t_v4_i + 3u]);
            const vec4 t_v5 = tanh(t_v4);
            const uint t_v5_o = (batch_v3 * pc.dst_nb3 + base_v12) / 4u;
            dst[t_v5_o] = t_v5.x;
            dst[t_v5_o + 1u] = t_v5.y;
            dst[t_v5_o + 2u] = t_v5.z;
            dst[t_v5_o + 3u] = t_v5.w;
        } else {
            for (uint col_tail_v6 = col_v0; col_tail_v6 < pc.n_col; ++col_tail_v6) {
                const float t_v7 = x[(col_tail_v6 * pc.x_nb0 + base_v9) / 4u];
                const float t_v8 = tanh(t_v7);
                dst[(col_tail_v6 * pc.dst_nb0 + base_v10) / 4u] = t_v8;
            }
        }
    }
}
