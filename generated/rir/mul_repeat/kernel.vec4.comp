// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: mul_repeat (backend vulkan, reduction serial, scan serial, variante vec4).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float a[]; };
layout(std430, binding = 1) readonly buffer Buf1 { float b[]; };
layout(std430, binding = 2) buffer Buf2 { float dst[]; };

layout(push_constant) uniform PC {
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint n_col_b;
    uint n_row_b;
    uint n_plane_b;
    uint n_batch_b;
    uint a_nb0;
    uint a_nb1;
    uint a_nb2;
    uint a_nb3;
    uint b_nb0;
    uint b_nb1;
    uint b_nb2;
    uint b_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x * 4u;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const uint rep_v14 = row_v1 % pc.n_row_b;
    const uint rep_v15 = plane_v2 % pc.n_plane_b;
    const uint base_v22 = col_v0 * pc.a_nb0 + row_v1 * pc.a_nb1 + plane_v2 * pc.a_nb2;
    const uint rep_v5 = col_v0 % pc.n_col_b;
    const uint base_v23 = rep_v5 * pc.b_nb0 + rep_v14 * pc.b_nb1 + rep_v15 * pc.b_nb2;
    const uint base_v24 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const uint base_v19 = row_v1 * pc.a_nb1 + plane_v2 * pc.a_nb2 + batch_v3 * pc.a_nb3;
        const uint rep_v16 = batch_v3 % pc.n_batch_b;
        const uint base_v20 = rep_v14 * pc.b_nb1 + rep_v15 * pc.b_nb2 + rep_v16 * pc.b_nb3;
        const uint base_v21 = row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2 + batch_v3 * pc.dst_nb3;
        if (col_v0 + 4u <= pc.n_col) {
            const uint t_v4_i = (batch_v3 * pc.a_nb3 + base_v22) / 4u;
            const vec4 t_v4 = vec4(a[t_v4_i], a[t_v4_i + 1u], a[t_v4_i + 2u], a[t_v4_i + 3u]);
            const uint t_v9_i = (rep_v16 * pc.b_nb3 + base_v23) / 4u;
            const vec4 t_v9 = vec4(b[t_v9_i], b[t_v9_i + 1u], b[t_v9_i + 2u], b[t_v9_i + 3u]);
            const vec4 t_v10 = t_v4 * t_v9;
            const uint t_v10_o = (batch_v3 * pc.dst_nb3 + base_v24) / 4u;
            dst[t_v10_o] = t_v10.x;
            dst[t_v10_o + 1u] = t_v10.y;
            dst[t_v10_o + 2u] = t_v10.z;
            dst[t_v10_o + 3u] = t_v10.w;
        } else {
            for (uint col_tail_v11 = col_v0; col_tail_v11 < pc.n_col; ++col_tail_v11) {
                const float t_v12 = a[(col_tail_v11 * pc.a_nb0 + base_v19) / 4u];
                const uint rep_v13 = col_tail_v11 % pc.n_col_b;
                const float t_v17 = b[(rep_v13 * pc.b_nb0 + base_v20) / 4u];
                const float t_v18 = t_v12 * t_v17;
                dst[(col_tail_v11 * pc.dst_nb0 + base_v21) / 4u] = t_v18;
            }
        }
    }
}
