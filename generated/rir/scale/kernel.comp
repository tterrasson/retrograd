// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: scale (backend vulkan, reduction serial, scan serial, variante repli).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float a[]; };
layout(std430, binding = 1) buffer Buf1 { float dst[]; };

layout(push_constant) uniform PC {
    float scale;
    float bias;
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint a_nb0;
    uint a_nb1;
    uint a_nb2;
    uint a_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x * 4u;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const uint base_v17 = row_v1 * pc.a_nb1 + plane_v2 * pc.a_nb2;
    const float p_v11 = pc.scale;
    const float p_v13 = pc.bias;
    const uint base_v18 = row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    const uint base_v19 = col_v0 * pc.a_nb0 + row_v1 * pc.a_nb1 + plane_v2 * pc.a_nb2;
    const uint base_v20 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const uint base_v15 = batch_v3 * pc.a_nb3 + base_v17;
        const uint base_v16 = batch_v3 * pc.dst_nb3 + base_v18;
        if (col_v0 + 4u <= pc.n_col) {
            const uint t_v4_i = (batch_v3 * pc.a_nb3 + base_v19) / 4u;
            const vec4 t_v4 = vec4(a[t_v4_i], a[t_v4_i + 1u], a[t_v4_i + 2u], a[t_v4_i + 3u]);
            const vec4 t_v6 = t_v4 * p_v11;
            const vec4 t_v8 = t_v6 + p_v13;
            const uint t_v8_o = (batch_v3 * pc.dst_nb3 + base_v20) / 4u;
            dst[t_v8_o] = t_v8.x;
            dst[t_v8_o + 1u] = t_v8.y;
            dst[t_v8_o + 2u] = t_v8.z;
            dst[t_v8_o + 3u] = t_v8.w;
        } else {
            for (uint col_tail_v9 = col_v0; col_tail_v9 < pc.n_col; ++col_tail_v9) {
                const float t_v10 = a[(col_tail_v9 * pc.a_nb0 + base_v15) / 4u];
                const float t_v12 = t_v10 * p_v11;
                const float t_v14 = t_v12 + p_v13;
                dst[(col_tail_v9 * pc.dst_nb0 + base_v16) / 4u] = t_v14;
            }
        }
    }
}
