// Generated by rir-gen - DO NOT EDIT MANUALLY.
// Kernel: add_repeat (backend vulkan, reduction serial, scan serial, variante repli).
// Strides are bytes in ggml order; parallel axes are mapped by invocation.
#version 450

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer Buf0 { float a[]; };
layout(std430, binding = 1) readonly buffer Buf1 { float b[]; };
layout(std430, binding = 2) buffer Buf2 { float dst[]; };

layout(push_constant) uniform PC {
    uint n_col;
    uint n_row;
    uint n_plane;
    uint n_batch;
    uint n_col_b;
    uint n_row_b;
    uint n_plane_b;
    uint n_batch_b;
    uint a_nb0;
    uint a_nb1;
    uint a_nb2;
    uint a_nb3;
    uint b_nb0;
    uint b_nb1;
    uint b_nb2;
    uint b_nb3;
    uint dst_nb0;
    uint dst_nb1;
    uint dst_nb2;
    uint dst_nb3;
} pc;

void main() {
    const uint col_v0 = gl_GlobalInvocationID.x;
    if (col_v0 >= pc.n_col) {
        return;
    }
    const uint row_v1 = gl_GlobalInvocationID.y;
    if (row_v1 >= pc.n_row) {
        return;
    }
    const uint plane_v2 = gl_GlobalInvocationID.z;
    if (plane_v2 >= pc.n_plane) {
        return;
    }
    const uint base_v11 = col_v0 * pc.a_nb0 + row_v1 * pc.a_nb1 + plane_v2 * pc.a_nb2;
    const uint rep_v5 = col_v0 % pc.n_col_b;
    const uint rep_v6 = row_v1 % pc.n_row_b;
    const uint rep_v7 = plane_v2 % pc.n_plane_b;
    const uint base_v12 = rep_v5 * pc.b_nb0 + rep_v6 * pc.b_nb1 + rep_v7 * pc.b_nb2;
    const uint base_v13 = col_v0 * pc.dst_nb0 + row_v1 * pc.dst_nb1 + plane_v2 * pc.dst_nb2;
    for (uint batch_v3 = 0u; batch_v3 < pc.n_batch; ++batch_v3) {
        const float t_v4 = a[(batch_v3 * pc.a_nb3 + base_v11) / 4u];
        const uint rep_v8 = batch_v3 % pc.n_batch_b;
        const float t_v9 = b[(rep_v8 * pc.b_nb3 + base_v12) / 4u];
        const float t_v10 = t_v4 + t_v9;
        dst[(batch_v3 * pc.dst_nb3 + base_v13) / 4u] = t_v10;
    }
}
